Only edit files directly related to the requested task.
Never modify unrelated parts of the project.

Do not create duplicate components.
If a component already exists — update it instead.

Never add unnecessary dependencies.
Keep the project clean and minimal.

Do not mix frameworks.
Use Astro components by default.
Use React/Svelte only if explicitly required.

Avoid creating dead code.
Remove unused imports, styles, or components.

Follow folder structure strictly.
Components → src/components
Layouts → src/components/Layout
Posts → src/content/posts
Pages → src/pages/[lang]

Always preserve TypeScript strictness.

Never break routing.

Follow Tailwind design rules:

use consistent spacing

rounded-xl

dark mode class

no inline styles unless necessary

Always preview changes.
Ensure that builds, formatting, and types all pass.

Never replace existing code unless instructed.

Always check compatiblity with Astro and Tailwind.