---
// Subcategory page - Russian version (rewrite to [lang])
const { category, subcategory } = Astro.params;
const response = await Astro.rewrite(`/ru/category/${category}/${subcategory}`);
return response;
---
